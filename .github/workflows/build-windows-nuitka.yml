name: Build Windows x64 Nuitka onefile

# 手动触发（不并入 build-dist.yml：Nuitka 编译 30-60 分钟，会让全平台构建超时）
# 方案：build-nuitka.ps1（Nuitka 4.1.3 onefile 编译）
on:
  workflow_dispatch:

jobs:
  build-nuitka:
    name: Windows x64 Nuitka onefile
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: 读取版本号
        id: ver
        shell: pwsh
        run: |
          $content = Get-Content "novelbase\__init__.py" -Raw
          $m = [regex]::Match($content, '__version__\s*=\s*"([^"]+)"')
          if (-not $m.Success) { Write-Error "无法从 novelbase\__init__.py 获取版本号"; exit 1 }
          $VERSION = $m.Groups[1].Value
          Write-Output "version=$VERSION" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
          Write-Host "版本号: $VERSION"

      - name: 安装依赖（供 Nuitka 静态分析）
        shell: pwsh
        run: pip install -r requirements.txt

      - name: Build Nuitka
        timeout-minutes: 120
        run: powershell -ExecutionPolicy Bypass -File ./scripts/build-nuitka.ps1 -Version ${{ steps.ver.outputs.version }}

      - name: 校验产物
        shell: pwsh
        run: |
          $exe = "dist\novel-crawler-web-${{ steps.ver.outputs.version }}.exe"
          Write-Host "dist 内容:"
          Get-ChildItem dist | Format-Table Name, Length -AutoSize
          if (-not (Test-Path $exe)) { Write-Error "未找到产物: $exe"; exit 1 }
          Write-Host "OK: $exe"

      - name: Defender 扫描
        shell: pwsh
        run: |
          $exe = "dist\novel-crawler-web-${{ steps.ver.outputs.version }}.exe"
          $defender = "C:\Program Files\Windows Defender\MpCmdRun.exe"
          if (Test-Path $defender) {
            Write-Host "Running Defender scan on $exe ..."
            & $defender -Scan -ScanType 3 -File $exe -DisableRemediation 2>&1 | Out-Host
            if ($LASTEXITCODE -ne 0) {
              Write-Warning "Defender scan exited with code $LASTEXITCODE (可能被拦截)"
            } else {
              Write-Host "Defender scan: clean"
            }
          } else {
            Write-Host "Defender 未安装或路径不可用，跳过扫描"
          }

      - name: Upload exe
        uses: actions/upload-artifact@v4
        with:
          name: novel-crawler-web-${{ steps.ver.outputs.version }}-windows-x64-exe
          path: dist/novel-crawler-web-${{ steps.ver.outputs.version }}.exe
          if-no-files-found: error
          retention-days: 3
